apiVersion: apps/v1
kind: Deployment
metadata:
  name: devops-project:1.0.0-28
spec:
  replicas: 2
  selector:
    matchLabels:
      app: devops-project:1.0.0-28
  template:
    metadata:
      labels:
        app: devops-project:1.0.0-28
    spec:
      containers:
        - name: devops-project:1.0.0-28
          image: mukeshr29/devops-project:1.0.0-28
          resources:
            limits:
              memory: "256Mi"
              cpu: "500m"
          ports:
            - containerPort: 8080
